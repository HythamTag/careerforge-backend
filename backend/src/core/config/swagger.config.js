/**
 * Swagger Configuration
 * 
 * Re-exports the modular swagger configuration.
 * @see ./swagger/index.js for the full configuration
 * 
 * @module core/config/swagger.config
 */

module.exports = require('./swagger');
